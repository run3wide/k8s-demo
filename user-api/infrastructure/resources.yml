apiVersion: v1
kind: Service
metadata:
  name: user-api
  labels:
    app: user-api
spec:
  selector:
    app: user-api
  ports:
    - port: 8080
      name: http
  type: NodePort
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-api-v1
  labels:
    app: user-api
spec:
  selector:
    matchLabels:
      app: user-api
  replicas: 1
  template:
    metadata:
      labels:
        app: user-api
        version: v1
    spec:
      containers:
        - name: user-api
          image: run3wide/user-api:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
            - name: BPL_JVM_HEAD_ROOM
              value: "2"
            - name: BPL_JVM_LOADED_CLASS_COUNT
              value: "35"
            - name: BPL_JVM_THREAD_COUNT
              value: "10"
            - name: JAVA_OPTS
              value: >-
                -XX:ReservedCodeCacheSize=40M
                -XX:MaxMetaspaceSize=60M
                -Xlog:gc
                -Xms34m
                -Xmx40m
                -Xss256k
                -XX:MaxRAM=150M
