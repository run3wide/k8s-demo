apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: order-api-vs
spec:
  hosts:
    - "*"
  gateways:
    - k8s-demo-gateway
  http:
    - match:
        - uri:
            prefix: "/"
      route:
        - destination:
            port:
              number: 8080
            host: order-api